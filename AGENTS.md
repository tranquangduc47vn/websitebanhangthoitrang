# Agent context — Webshop

When the user asks to **continue** (*tiếp tục*), read **`docs/SESSION-HANDOFF.md`** first.

Also see: `docs/PROJECT-STATUS.md`, `docs/URL-MIGRATION.md`, `CHANGELOG_STRUCTURE.md`.

Cart data lives in session **`custom_cart`**. Do not commit unless the user asks.
